# ============================================================
# WARNING: CI-ONLY CONFIGURATION — DO NOT USE IN PRODUCTION
# ============================================================
# This broker accepts anonymous connections on all interfaces
# (0.0.0.0:1883) with no authentication or encryption.
# It exists SOLELY for the /healthz smoke step in
# .github/workflows/release.yml and must never be used in
# any non-CI environment.
# ============================================================
listener 1883 0.0.0.0
allow_anonymous true
